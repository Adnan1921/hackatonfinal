<%= form_with(model: mapatestiranja, local: true) do |form| %>
  <% if mapatestiranja.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mapatestiranja.errors.count, "error") %> prohibited this mapatestiranja from being saved:</h2>

      <ul>
        <% mapatestiranja.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
